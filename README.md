Add massless charge to water for TIP4P model
---------------------------------------------

To compile, use the Makefile:
`make`

This makes the program `add_massless_charge`

Make changes to the in.data file and then just run `./add_massless_charge`
